# SocialAPIs MCP Server Configuration
# Copy this file to .env and fill in your values

# Environment (development or production)
NODE_ENV=production

# Port for HTTP proxy server (default: 3001)
PORT=3001

# SocialAPIs backend API URL
API_BASE_URL=https://api.socialapis.io/v1

# Optional: MCP proxy URL (for custom deployments)
# MCP_PROXY_URL=https://mcp.socialapis.io

# Optional: Your SocialAPIs API key (can also be passed as argument)
# SOCIALAPIS_API_KEY=your_api_key_here